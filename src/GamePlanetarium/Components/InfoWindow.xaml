<Window x:Class="GamePlanetarium.Components.InfoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="InformationWindow" Height="702" Width="1248"
        WindowStyle="None"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen" 
        AllowsTransparency="True" Background="{x:Null}"
        Style="{StaticResource WithDefaultFont}">
    <Border Style="{StaticResource BorderWithShadow}"
            Background="White"
            CornerRadius="20"
            BorderBrush="DarkGray"
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <Button Grid.Row="0" Grid.Column="1" x:Name="CloseButton"  
                    Style="{StaticResource CloseButton}"
                    TouchUp="CloseButton_OnTouchUp"
                    Click="CloseButton_OnClick"/>
            <TextBlock Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" x:Name="MessageTextBlock"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       TextAlignment="Center"
                       Margin="30"
                       TextWrapping="Wrap"
                       FontSize="40"
                       Text="{Binding Message}"/>
            <Image Grid.Row="0" Grid.Column="1" Grid.RowSpan="2"
                   HorizontalAlignment="Right"
                   VerticalAlignment="Bottom"
                   Source = "{Binding InfoImageSource}"
                   MaxHeight="450"/>
        </Grid>
    </Border>
</Window>

